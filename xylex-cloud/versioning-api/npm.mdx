---
title: 'NPM & Node version'
api: 'GET https://xylex.cloud/api/version'
---

<Card title="Endpoint Description" icon="info" horizontal>
Returns the `npm` and `node` versions active on the target machine
</Card>

<Card title="SSH" icon="exclamation" horizontal>
Not every `SSH` user will have the same permissions nor packages installed by `apt-get` so be mindful
</Card>

