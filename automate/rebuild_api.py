# import subprocess

# def run_api_v2_script():
#     script_path = "api_v2.sh"
#     try:
#         result = subprocess.run(f"cd /home/floris-xlx && ./{script_path}", shell=True, check=True, capture_output=True, text=True)
#         print("Script output:", result.stdout)
#     except subprocess.CalledProcessError as e:
#         print("An error occurred while running the script:", e.stderr)

# # run_api_v2_script()
