---
title: 'User'
api: 'GET https://api.v2.xylex.ai/public/user'
---



<Card title="Endpoint Description" icon="info" horizontal >Fetches user profile data by username, checking cache first, then database if necessary, and returns the profile with response time.</Card>





<ParamField query="username" type="String" required  children="This parameter specifies the unique username of the user whose profile is to be fetched. It is required to identify and retrieve the respective user profile from the cache or database.">
  The username for which to fetch the user profile.
</ParamField>
